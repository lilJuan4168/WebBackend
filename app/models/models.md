<p>
aqui se crean las tablas de la base de datos desde el mismo python, un archivo para cada tabla.

estas tablas de generaran automaticamente desde un script (create_db.py) desde la carpeta scripts.
</p>